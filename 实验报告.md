# MNIST 分类实验报告（MLP / CNN / 拓展）

本文档为 **Markdown** 源码（扩展名 `.md`，遵循常见 **GFM** 表格与标题语法）。在 Cursor / VS Code 中可打开预览；若需 PDF 或 Word，可用 **Pandoc**（例：`pandoc 实验报告.md -o 实验报告.pdf`）或各编辑器导出。

---

**课程**：神经网络 · **实现**：NumPy（`codes/mynn`）  
**数据**：MNIST（`codes/dataset/MNIST/`，缺文件时 `fetch_mnist` 可自动下载）

---

## 1. 实验目的

1. 在 MNIST 上实现并训练 **全连接网络（MLP）** 与 **卷积网络（CNN）**，比较二者在相同优化设定下的验证集与官方测试集表现。  
2. 在固定协议下开展 **Part C 拓展**：**恒定学习率基线**、**余弦退火学习率**、**Dropout 正则**，分别在 MLP 与 CNN 上观察趋势。  
3. 熟悉训练—验证—测试流程、checkpoint 选择与指标记录方式。

---

## 2. 数据与划分

- **训练图像**：`train-images-idx3-ubyte.gz`，标签对应 `train-labels-idx1-ubyte.gz`。  
- **官方测试集**：`t10k-images-idx3-ubyte.gz` / `t10k-labels-idx1-ubyte.gz`（**仅用于最终评估**，脚本中通过 `--eval_test` 在训练结束后用 **best dev** 权重评测）。  
- **训练 / 验证划分**：在打乱后的 60k 训练样本上，取 **前 10000** 作为 **验证集（dev）**，后 **50000** 作为训练集；划分由随机种子与 `idx.pickle` 固定。**Part B / Part C** 使用 `--reuse_idx` 与 Part A 共用同一划分，保证可比。

---

## 3. 模型与训练设置概要

### 3.1 MLP（Part A）

- 结构（见 `test_train.build_model`）：**784 → 600 → 10**，隐藏层 **ReLU**，输出层接 **Softmax + 交叉熵**（由 `MultiCrossEntropyLoss` 实现）。  
- 优化：**SGD**，初始学习率 **0.06**，**无**学习率调度，**无** Dropout（Part A）。

### 3.2 CNN（Part B）

- 结构见 `mynn/models.py` 中 `Model_CNN`（卷积 + 池化 + 全连接等，与作业给定实现一致）。  
- 优化设定与 Part A **对齐**（公平对比）：batch、lr、epoch、seed、scheduler、dropout 等与 MLP 一致，仅模型不同。

### 3.3 监控与保存

- **验证**：`eval_interval=100` 时，约**每 100 个 training iteration** 在 dev 上评估一次（见 `mynn/runner.py`）；曲线写入 `train_*` / `dev_*` 序列。  
- **最优模型**：以 **dev 准确率最高** 为准则保存 `best_model.pickle`。  
- **曲线与数值**：`--save_fig` 保存 PNG，`--save_metrics` 保存 `.npz`（含曲线与 `best_score`，若开启 `--eval_test` 则含 `test_acc` / `test_loss`）。

---

## 4. 实验配置表（与当前脚本一致）

| 项目 | Part A（MLP） | Part B（CNN） | Part C（每组） |
|------|----------------|----------------|----------------|
| Epoch | 8 | 8 | 5（默认） |
| Batch size | 64 | 64 | 64 |
| 优化器 | SGD，lr=0.06 | 同左 | 同左 |
| Seed | 309 | 309 | 309 |
| Scheduler | none | none | baseline：none；cosine：**CosineAnnealingLR（step_on=epoch，η_min=0）**；dropout 组：none |
| Dropout | 0 | 0 | baseline/cosine：0；正则组：**0.5**（隐层 FC 路径） |
| `eval_interval` | 100 | 100 | 100 |
| `log_iters` | 100 | 100 | 100 |
| 测试集评估 | `--eval_test` | `--eval_test` | `--eval_test` |

**Part C 默认**：`python run_part_c.py` 对 **MLP 与 CNN 各跑三轮**（baseline → cosine → dropout），并生成 `figs/part_c_{mlp,cnn}_overlay.png`。

**一键复现（`codes/` 目录）**：`python run_pipeline.py`（Part A → Part B → MLP/CNN dev 叠图 → Part C）。

---

## 5. 实验结果

### 5.1 Part A / Part B（MLP vs CNN）

运行结束后，将终端中的 **`[mlp]` / `[cnn]` best dev** 与 **test accuracy / test loss** 填入下表，并在报告中粘贴或插入图像路径对应的图。

| 模型 | Best dev acc | Test acc | Test loss |
|------|----------------|-----------|-----------|
| MLP | （填写） | （填写） | （填写） |
| CNN | （填写） | （填写） | （填写） |

**建议插图**（生成于 `codes/figs/`）：

- `part_a_mlp_learning_curve.png`、`part_b_cnn_learning_curve.png`  
- `part_ab_mlp_vs_cnn_overlay.png`（若已跑 `run_pipeline.py`）

**简要分析（自行撰写）**：可从收敛速度、过拟合迹象、CNN 相对 MLP 的 dev/test 差异、曲线形状等方面概括。

### 5.2 Part C（组内对比）

对 **MLP** 与 **CNN** 分别填写三组实验的 best dev 与 test（可与终端 `[Part C summary]` 一致）：

**MLP**

| 设置 | Dev（best） | Test acc | Test loss |
|------|-------------|-----------|-----------|
| Baseline | | | |
| Cosine LR | | | |
| Dropout 0.5 | | | |

**CNN**

| 设置 | Dev（best） | Test acc | Test loss |
|------|-------------|-----------|-----------|
| Baseline | | | |
| Cosine LR | | | |
| Dropout 0.5 | | | |

**建议插图**：`figs/part_c_mlp_overlay.png`、`figs/part_c_cnn_overlay.png`。

**简要分析（自行撰写）**：  
- 余弦退火是否在 **Part C 当前 epoch** 设置下带来 dev/test 改善或更平滑的曲线；  
- Dropout 对训练曲线与泛化（相对 baseline）的影响是否符合预期；  
- 同一策略在 MLP 与 CNN 上差异的原因（参数量、归纳偏置等，言之有理即可）。

---

## 6. 结论

（用 3～5 句话总结：）MLP 与 CNN 在相同设定下的整体对比；Part C 两个方向各自是否达到预期；若结果与预期不符，可列举可能原因（epoch 数、学习率、验证频率、实现细节等）。

---

## 7. 复现命令摘要

```text
cd codes
python run_part_a.py
python run_part_b.py
python run_part_c.py          # 默认 MLP+CNN，Part C 各 5 epoch
# 或
python run_pipeline.py
```

---

## 8. 声明

本报告中 **待填数值与讨论** 须根据你本机实际运行日志与 `figs/`、`best_models/` 产物填写；上述超参与脚本路径与仓库当前版本一致，若你修改了 `run_part_*.py`，请以实际脚本为准更新第 4 节表格。
